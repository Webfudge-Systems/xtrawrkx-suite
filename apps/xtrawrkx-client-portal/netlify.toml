[build]
  base = "xtrawrkx-client-portal"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Environment variables for staging/develop
# Add these in Netlify dashboard under Site settings → Environment variables
# NEXT_PUBLIC_API_URL
# NEXT_PUBLIC_APP_ENV

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

